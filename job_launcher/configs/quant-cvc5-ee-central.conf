# R15: test the existing central equality-engine architecture.
COMMENT="heuresis R15: best-known cvc5 configuration with central equality engine"
TAG=u-ssc-ee-central

BRANCH=
DIR=$QUANT_DIR
DRIVER=solve_dir_rec_par_cvc5
TIMEOUT=30
BINARY=$CVC5_BIN
SCRIPT=cvc5_solve.sh
OPTS="-q --no-cbqi --user-pat=strict --sat-solver=cadical --ee-mode=central"
