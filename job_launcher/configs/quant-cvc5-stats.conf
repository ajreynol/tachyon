# cvc5 --stats-internal on the set, in the configuration under study: the raw
# material for the attribution table (heuresis goal 2).  Output goes to the
# host's ~/analysis/stats.
COMMENT="heuresis attribution: internal statistics on the set"
TAG=u-ssc

BRANCH=
DIR=$QUANT_DIR
DRIVER=stats_dir_rec_par_cvc5
TIMEOUT=30
BINARY=$CVC5_BIN
OPTS="-q --no-cbqi --user-pat=strict --sat-solver=cadical"
