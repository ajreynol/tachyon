# R1/R28: bounded eager instantiation at branch defaults (--eager-inst-gen-limit=1,
# --eager-inst-pair-limit=2000, --eager-inst-multi=true, --eager-inst-merge=false),
# on top of the strongest measured configuration.
COMMENT="heuresis R1/R28: claude-eagerInst eager instantiation at branch defaults"
TAG=u-ssc-eager-inst

BRANCH=claude-eagerInst
DIR=$QUANT_DIR
DRIVER=solve_dir_rec_par_cvc5
TIMEOUT=30
BINARY=$REPO_BINARY
SCRIPT=cvc5_solve.sh
OPTS="-q --no-cbqi --user-pat=strict --sat-solver=cadical --ee-mode=central --ieval=off --eager-inst"
