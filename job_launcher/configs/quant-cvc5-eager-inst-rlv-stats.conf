# R1/R28 (S2): the same counters for the relevancy-deferred arm.  The two arms
# rescue largely disjoint sets (29 together, 5 shared), so both are needed.
COMMENT="heuresis R1/R28 (S2): eager-instantiation counters with relevancy deferral"
TAG=u-ssc-eager-inst-rlv

BRANCH=claude-eagerInst
DIR=$QUANT_DIR
DRIVER=stats_dir_rec_par_cvc5
TIMEOUT=30
BINARY=$REPO_BINARY
OPTS="-q --no-cbqi --user-pat=strict --sat-solver=cadical --ee-mode=central --ieval=off --eager-inst --eager-inst-rlv"
