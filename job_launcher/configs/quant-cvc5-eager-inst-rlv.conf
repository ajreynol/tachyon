# R1/R28: eager instantiation with the relevancy approximation, which defers
# matching of terms in singleton equivalence classes until they participate in
# a merge.  Isolates one knob against quant-cvc5-eager-inst.conf.
COMMENT="heuresis R1/R28: claude-eagerInst eager instantiation with relevancy deferral"
TAG=u-ssc-eager-inst-rlv

BRANCH=claude-eagerInst
DIR=$QUANT_DIR
DRIVER=solve_dir_rec_par_cvc5
TIMEOUT=30
BINARY=$REPO_BINARY
SCRIPT=cvc5_solve.sh
OPTS="-q --no-cbqi --user-pat=strict --sat-solver=cadical --ee-mode=central --ieval=off --eager-inst --eager-inst-rlv"
