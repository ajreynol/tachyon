# cvc5 on the set with default options: what the best known configuration in
# tools/heuresis/notes.md is worth, measured rather than remembered.
COMMENT="heuresis baseline: cvc5 default options on the set"
TAG=default

BRANCH=
DIR=$QUANT_DIR
DRIVER=solve_dir_rec_par_cvc5
TIMEOUT=30
BINARY=$CVC5_BIN
SCRIPT=cvc5_solve.sh
OPTS="-q"
