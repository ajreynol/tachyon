# cvc5 on the set with default options: what the "things that helped" in
# tools/heuresis/notes.md are worth, measured rather than remembered.
COMMENT="heuresis: cvc5 default options on the set, for comparison with u-ssc"
TAG=default

BRANCH=
DIR=$QUANT_DIR
DRIVER=solve_dir_rec_par_cvc5
TIMEOUT=60
BINARY=$CVC5_BIN
SCRIPT=cvc5_solve.sh
OPTS="-q"
