# R1/R28 (S2): the eager module's own counters -- d_statPairs, d_statMatches,
# d_statInst, d_statRematch, d_procTime, d_addInstTime -- for the arm measured
# at -19.46% PAR2.  Needed to give both the loss and the 29 rescues a
# mechanism.  Output goes to the host's ~/analysis/stats.
COMMENT="heuresis R1/R28 (S2): eager-instantiation counters at branch defaults"
TAG=u-ssc-eager-inst

BRANCH=claude-eagerInst
DIR=$QUANT_DIR
DRIVER=stats_dir_rec_par_cvc5
TIMEOUT=30
BINARY=$REPO_BINARY
OPTS="-q --no-cbqi --user-pat=strict --sat-solver=cadical --ee-mode=central --ieval=off --eager-inst"
