# Quantifier-pattern control: isolate strict user patterns from --no-cbqi.
COMMENT="heuresis quantifier control: cvc5 with strict user patterns alone"
TAG=user-pat-strict

BRANCH=
DIR=$QUANT_DIR
DRIVER=solve_dir_rec_par_cvc5
TIMEOUT=30
BINARY=$CVC5_BIN
SCRIPT=cvc5_solve.sh
OPTS="-q --user-pat=strict"
