# R16: test the mainline binary datatype split before maintaining the
# relevance-gated split branch.
COMMENT="heuresis R16: best-known cvc5 configuration with binary datatype splits"
TAG=u-ssc-dt-binary

BRANCH=
DIR=$QUANT_DIR
DRIVER=solve_dir_rec_par_cvc5
TIMEOUT=30
BINARY=$CVC5_BIN
SCRIPT=cvc5_solve.sh
OPTS="-q --no-cbqi --user-pat=strict --sat-solver=cadical --dt-binary-split"
