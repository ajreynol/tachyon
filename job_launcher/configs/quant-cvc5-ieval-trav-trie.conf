# R7: test trie-aware partial instantiation evaluation against current main's
# central-equality evaluator-on comparator.
COMMENT="heuresis R7: rebased ievalTravTrie with central equality"
TAG=u-ssc-ieval-trav-trie

BRANCH=ievalTravTrie
DIR=$QUANT_DIR
DRIVER=solve_dir_rec_par_cvc5
TIMEOUT=30
BINARY=$REPO_BINARY
SCRIPT=cvc5_solve.sh
OPTS="-q --no-cbqi --user-pat=strict --sat-solver=cadical --ee-mode=central"
