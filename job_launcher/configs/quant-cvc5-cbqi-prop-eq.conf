# R6/R28 mainline proxy: explicitly enable QCF's default propagation/equality
# effort while keeping strict user patterns and explicit CaDiCaL.
COMMENT="heuresis R6/R28: strict patterns with propagation/equality CBQI"
TAG=u-sc-cbqi-prop-eq

BRANCH=
DIR=$QUANT_DIR
DRIVER=solve_dir_rec_par_cvc5
TIMEOUT=30
BINARY=$CVC5_BIN
SCRIPT=cvc5_solve.sh
OPTS="-q --user-pat=strict --sat-solver=cadical --cbqi-mode=prop-eq"
