# progress.md tracked configuration `verus`, at the cvc5 1.4.0 release point.
# master@a07d513075 is "Start post-release for 1.4.0"; it differs from the
# cvc5-1.4.0 tag only in cmake/version-base.cmake, so this measures the release.
COMMENT="heuresis progress: verus configuration at the 1.4.0 release"
TAG=u-ssc-140

BRANCH=
DIR=$QUANT_DIR
DRIVER=solve_dir_rec_par_cvc5
TIMEOUT=30
BINARY=$REPO_BINARY
SCRIPT=cvc5_solve.sh
OPTS="-q --no-cbqi --user-pat=strict --sat-solver=cadical"
